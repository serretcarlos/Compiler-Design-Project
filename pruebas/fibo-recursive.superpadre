program fibonacci;
int fibo(int n ) {
  var int result;
  if ( n <= 1) {
    result =  n;
  }
  else {
    result = fibo(n- 1) + fibo(n- 2);
  }
}
main (){
  var int n;
  cread( n);
  n = fibo(n);
  cwrite( n);

}